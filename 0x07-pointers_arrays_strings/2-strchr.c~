#include "holberton.h"

char *_strchr(char *s, char c)
{
	int i, j;

	for (i = 0; s[i] != '\0'; ++i)
	{
		if (s[i] == c)
			break;
		else
			return (0);
	}
	for (j = 0; s[j] != '\0'; j++, i++)
		s[j] = s[i];
	return(s);



}
