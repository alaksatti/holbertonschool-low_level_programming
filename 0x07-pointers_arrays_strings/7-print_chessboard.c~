#include "holberton.h"

/**
 * print_chessboard - write a function that prints a chessboard.
 * @a: Number of rows.
 * Return: nothing.
 */

void print_chessboard(char (*a)[8])
{

	for (i = 0; i < a; i++)
		for (j = 0; j < 8; j++)
			_putchar(a[i][j]);









}
