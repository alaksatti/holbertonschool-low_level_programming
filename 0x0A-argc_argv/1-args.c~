#include <stdio.h>


/**
 * main - prints the number of arguments passed to it.
 * @argc: argument coount.
 * @argc: vector of strings passed to program.
 * Return: 0 upon successful completion.
 */


int main(int argc, char *argv[])
{
	(void)argv;

	printf("%i\n", argc - 1);




	return (0);

}
