#include "holberton.h"


/**
 * print_square - prints a square.
 * @size: size of square.
 * Return: nothing;
 */


void print_square(int size)
{

	if (size <= 0)
		_putchar('\n');

	for (i = 1; i <= n; i++)
		for (j = 1; j <= n; j++)
			_putchar('\#');

	_putchar('\n');
}
